# Server-side only (Next.js API routes proxy to Railway at runtime — required on Vercel)
FINTRACE_API_URL=https://your-core-finance.up.railway.app

# Local development
# FINTRACE_API_URL=http://localhost:5280
